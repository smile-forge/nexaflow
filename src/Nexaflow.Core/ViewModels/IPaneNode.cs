namespace Nexaflow.Core.ViewModels;

/// <summary>
/// Marker for anything that can be the root content of a window:
/// today only <see cref="Pane"/>; tomorrow a SplitPaneNode = left + right panes.
/// </summary>
public interface IPaneNode { }
