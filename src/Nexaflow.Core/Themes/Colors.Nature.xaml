<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ── Palette: sunlit-forest GLASS ──
         Immersive woodland theme (like Ocean): a dappled-forest backdrop (Scene.Window) behind
         deep mossy-green translucent panels with warm cream text and a honey-gold "sunlight" accent.
         The page root is translucent so the forest reads straight through feature pages. -->
    <Color x:Key="BgColor">#5012301B</Color>       <!-- page root: mossy glass over the forest -->
    <Color x:Key="SurfaceColor">#16331F</Color>     <!-- panels / cards -->
    <Color x:Key="Surface2Color">#1F4A2B</Color>    <!-- raised panels / inputs -->
    <Color x:Key="BorderColor">#2D5638</Color>
    <Color x:Key="BorderLightColor">#48794F</Color>
    <Color x:Key="AccentColor">#EBB749</Color>
    <!-- Honey-gold dappled sunlight -->
    <Color x:Key="Accent2Color">#B6E25A</Color>
    <!-- Fresh leaf-green -->
    <Color x:Key="TextColor">#F2F7EA</Color>
    <Color x:Key="TextMutedColor">#A2B89F</Color>
    <Color x:Key="DeepBgColor">#0A1A10</Color>

    <!-- ── Brushes ── -->
    <SolidColorBrush x:Key="BgBrush"         Color="{StaticResource BgColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush"    Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="Surface2Brush"   Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="BorderBrush"     Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="BorderLightBrush" Color="{StaticResource BorderLightColor}"/>
    <SolidColorBrush x:Key="AccentBrush"     Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="Accent2Brush"    Color="{StaticResource Accent2Color}"/>
    <SolidColorBrush x:Key="TextBrush"       Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="TextMutedBrush"  Color="{StaticResource TextMutedColor}"/>
    <SolidColorBrush x:Key="DeepBgBrush"     Color="{StaticResource DeepBgColor}"/>

    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#F2C94C" Offset="0"/>
        <GradientStop Color="#B6E25A" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="BarGradientBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
        <GradientStop Color="{StaticResource Accent2Color}" Offset="1"/>
    </LinearGradientBrush>

    <!-- Dimensions -->
    <GridLength x:Key="TopBarHeight">72</GridLength>
    <GridLength x:Key="TabBarHeight">38</GridLength>
    <GridLength x:Key="InteractionHeight">90</GridLength>

</ResourceDictionary>
