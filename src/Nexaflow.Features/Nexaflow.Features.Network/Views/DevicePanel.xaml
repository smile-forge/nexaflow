<UserControl x:Class="Nexaflow.Features.Network.Views.DevicePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.AutomationId="Net_DevicePanel">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Border Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{Binding Selected.Name}"
                       Foreground="{StaticResource TextBrush}"
                       FontSize="15" FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis" />

            <!-- Whatever applies to THIS device. The page holds no list of buttons: each action was asked
                 whether it applies, and one that cannot work is never offered. -->
            <ItemsControl Grid.Row="1" Margin="0,10,0,0" ItemsSource="{Binding Actions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource TbBtn}"
                                Margin="0,0,6,6"
                                Command="{Binding RunCommand}"
                                ToolTip="{Binding Description}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}" Margin="0,0,6,0" />
                                <TextBlock Text="{Binding DisplayName}" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Grid.Row="2"
                       Text="{Binding ActionResult}"
                       Margin="0,2,0,8"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource AccentBrush}" />

            <!-- Every fact, and where it came from. This is the column a grid cannot have: which layer
                 said so, how sure it was, and how long ago. -->
            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Facts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="0,5"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="118" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Label}"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               FontSize="11"
                                               TextWrapping="Wrap" />

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Value}"
                                                   Foreground="{StaticResource TextBrush}"
                                                   TextWrapping="Wrap" />

                                        <StackPanel Orientation="Horizontal" Margin="0,1,0,0">
                                            <TextBlock Text="{Binding Source}"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       FontSize="10"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Confidence}"
                                                       Margin="8,0,0,0"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       FontSize="10" />
                                            <TextBlock Text="{Binding Age}"
                                                       Margin="8,0,0,0"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       FontSize="10" />
                                            <!-- Two layers disagreed. The graph has always been able to
                                                 say so and nothing has ever shown it. -->
                                            <TextBlock Text="contested"
                                                       Margin="8,0,0,0"
                                                       FontSize="10"
                                                       Foreground="{StaticResource WarningBrush}"
                                                       Visibility="{Binding Contested,
                                                                    Converter={StaticResource BoolToVis}}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
