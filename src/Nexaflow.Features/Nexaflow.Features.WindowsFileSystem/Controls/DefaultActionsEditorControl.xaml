<UserControl x:Class="Nexaflow.Features.WindowsFileSystem.Controls.DefaultActionsEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cv="clr-namespace:Nexaflow.Visuals.Common.Converters;assembly=Nexaflow.Visuals.Common"
             xmlns:loc="clr-namespace:Nexaflow.Visuals.Common.Localization;assembly=Nexaflow.Visuals.Common">

    <UserControl.Resources>
        <cv:BoolToVisibilityConverter        x:Key="BoolVis"/>
        <cv:InverseBoolToVisibilityConverter x:Key="InverseBoolVis"/>
    </UserControl.Resources>

    <Grid Margin="12">

        <!-- ── List view: configured overrides ─────────────────────────────── -->
        <DockPanel Visibility="{Binding IsAdding, RelativeSource={RelativeSource AncestorType=UserControl},
                                        Converter={StaticResource InverseBoolVis}}">
            <TextBlock DockPanel.Dock="Top" Text="{loc:Str WindowsFileSystem.DefaultActions.Title}"
                       Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" FontSize="13"/>
            <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Margin="0,4,0,10"
                       Foreground="{StaticResource TextMutedBrush}" FontSize="11"
                       Text="{loc:Str WindowsFileSystem.DefaultActions.Intro}"/>

            <Button DockPanel.Dock="Top" HorizontalAlignment="Left" Content="{loc:Str WindowsFileSystem.DefaultActions.Add}"
                    MinWidth="110" Margin="0,0,0,8" Style="{StaticResource IconButton}" Click="Add_Click" AutomationProperties.AutomationId="DefaultActions_Add"/>

            <TextBlock DockPanel.Dock="Top" Text="{loc:Str WindowsFileSystem.DefaultActions.Empty}"
                       Foreground="{StaticResource TextMutedBrush}" FontSize="11"
                       Visibility="{Binding HasNoOverrides, RelativeSource={RelativeSource AncestorType=UserControl},
                                            Converter={StaticResource BoolVis}}"/>

            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4"
                    Visibility="{Binding HasNoOverrides, RelativeSource={RelativeSource AncestorType=UserControl},
                                         Converter={StaticResource InverseBoolVis}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="SummaryList" Margin="2">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Extension}" VerticalAlignment="Center"
                                               Foreground="{StaticResource TextBrush}" FontSize="12"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Label}" VerticalAlignment="Center"
                                               Foreground="{StaticResource TextMutedBrush}" FontSize="12"
                                               TextTrimming="CharacterEllipsis"/>
                                    <Button Grid.Column="2" Content="{loc:Str WindowsFileSystem.DefaultActions.Edit}" Margin="4,0,0,0"
                                            Style="{StaticResource IconButton}" Tag="{Binding}" Click="Edit_Click" AutomationProperties.AutomationId="DefaultActions_Edit"/>
                                    <Button Grid.Column="3" Margin="4,0,0,0" ToolTip="{loc:Str WindowsFileSystem.DefaultActions.Remove}"
                                            Style="{StaticResource IconButton}" Tag="{Binding}" Click="Remove_Click" AutomationProperties.AutomationId="DefaultActions_Remove">
                                        <TextBlock Text="✕" FontSize="10" Foreground="{StaticResource TextMutedBrush}"/>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </DockPanel>

        <!-- ── Add / edit view ──────────────────────────────────────────────── -->
        <DockPanel Visibility="{Binding IsAdding, RelativeSource={RelativeSource AncestorType=UserControl},
                                        Converter={StaticResource BoolVis}}">
            <TextBlock x:Name="AddTitle" DockPanel.Dock="Top" Text="{loc:Str WindowsFileSystem.DefaultActions.AddTitle}"
                       Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" FontSize="13"/>

            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,8,0,8">
                <TextBlock Text="{loc:Str WindowsFileSystem.DefaultActions.Extension}" VerticalAlignment="Center"
                           Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,8,0"/>
                <TextBox x:Name="ExtBox" Width="160" Style="{StaticResource DarkTextBox}" FontSize="12"
                         TextChanged="ExtBox_TextChanged" ToolTip="{loc:Str WindowsFileSystem.DefaultActions.Extension.Tooltip}" AutomationProperties.AutomationId="DefaultActions_Extension"/>
            </StackPanel>

            <TextBlock x:Name="CurrentExtLabel" DockPanel.Dock="Top" Text="{loc:Str WindowsFileSystem.DefaultActions.EnterExtension}"
                       Foreground="{StaticResource TextBrush}" FontSize="12" Margin="0,0,0,4"/>

            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="{loc:Str WindowsFileSystem.Common.Cancel}" MinWidth="80" Margin="0,0,8,0"
                        Style="{StaticResource IconButton}" Click="Cancel_Click" AutomationProperties.AutomationId="DefaultActions_Cancel"/>
                <Button Content="{loc:Str WindowsFileSystem.DefaultActions.Confirm}" MinWidth="90" Style="{StaticResource IconButton}" Click="Confirm_Click"
                        IsEnabled="{Binding CanConfirm, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        AutomationProperties.AutomationId="DefaultActions_Confirm"/>
            </StackPanel>

            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                <ListBox x:Name="CandidateList" Background="Transparent" BorderThickness="0" AutomationProperties.AutomationId="DefaultActions_Candidates">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="2">
                                <TextBlock Text="{Binding Label}"
                                           Foreground="{StaticResource TextBrush}" FontSize="12"/>
                                <TextBlock Text="{Binding Group}"
                                           Foreground="{StaticResource TextMutedBrush}" FontSize="10"
                                           Visibility="{Binding HasGroup, Converter={StaticResource BoolVis}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </DockPanel>
    </Grid>
</UserControl>
