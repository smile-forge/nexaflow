<UserControl x:Class="Nexaflow.Visuals.Web.Controls.WebSurface"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">

    <!-- No AutomationId on the WebView2: it is an HwndHost, so its native browser window is not part of
         the hosting window's WPF automation tree and an id here would never surface. A UI test instead
         reads the rendered document, which WebView2 does expose to UIA (see WebJourneyTests). -->
    <Grid>
        <wv2:WebView2 x:Name="View"/>
    </Grid>
</UserControl>
