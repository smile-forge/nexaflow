<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>latex-corpus</AssemblyName>
    <RootNamespace>Nexaflow.Tools.LatexCorpus</RootNamespace>
    <!-- A survey harness, not a shipping component: it is deliberately out of Nexaflow.slnx. -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Nexaflow.Visuals.Maths\Nexaflow.Visuals.Maths.csproj" />
  </ItemGroup>

</Project>
